import { TransformReponseInterceptor } from './transform-reponse.interceptor';

describe('TransformReponseInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformReponseInterceptor()).toBeDefined();
  });
});
