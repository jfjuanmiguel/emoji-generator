import { TransformResponseInterceptor } from './transform-response-interceptor.service';

describe('TransformResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformResponseInterceptor()).toBeDefined();
  });
});
